# Contributing

Catalog changes are release operations rather than ordinary content edits. Every commit requires Developer Certificate of Origin 1.1 sign-off with `git commit -s`, successful catalog verification, and approval by the signing-key custodian.

Never commit a private key, passphrase, unsigned replacement catalog, or mutable download URL.
